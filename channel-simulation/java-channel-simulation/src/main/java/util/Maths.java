package util;

public class Maths {
    public static float log2(float n) {
        return (float) (Math.log(n) / Math.log(2));
    }

    public static float log2(int n) {
        return (float) (Math.log(n) / Math.log(2));
    }
}
